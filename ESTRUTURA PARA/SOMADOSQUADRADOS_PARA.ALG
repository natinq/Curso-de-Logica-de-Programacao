Algoritmo "SomadosQuadrados_PARA"
// Apresentar o total da soma dos quadrados dos números inteiros de 2 a 5.
Var

   quadrado, soma, i: inteiro
Inicio
      soma <- 0
      quadrado <- 0
      
      para i de 2 ate 5 faca
      quadrado <- i * i
      soma <- soma + quadrado
      fimpara
      escreva("Soma dos quadrados = ",soma)


Fimalgoritmo